accelerate>=1.7.0
bitsandbytes==0.46.0
click==8.1.7
grpcio==1.72.1
grpcio-tools==1.64.0
peft==0.17.1  # pinned: paper measured on 0.17.1; a floor (>=) drifts to newer PEFT (0.19.x) whose add_adapter path is slower on cold-start adapter load. See RQ2_cold_start_results.md.
torch>=2.7.0,<2.11.0
transformers>=4.52.4,<5  # 5.x removed PreTrainedModel._get_no_split_modules, which sllm_store's device_map loader requires
